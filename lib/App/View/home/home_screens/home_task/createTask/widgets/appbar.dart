import 'package:flutter/material.dart';
import 'package:get/get.dart';

AppBar appbartask() {
  return AppBar(
    title: Text(
      "Create Task".tr,
      style: TextStyle(color: Colors.white),
    ),
    centerTitle: true,
  );
}

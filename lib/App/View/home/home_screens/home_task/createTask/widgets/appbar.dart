import 'package:flutter/material.dart';

AppBar appbartask() {
  return AppBar(
    title: const Text(
      "Create Task",
      style: TextStyle(color: Colors.white),
    ),
    centerTitle: true,
  );
}
